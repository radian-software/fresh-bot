name: "Fresh-bot"
author: "c-hive"
description: ""
runs:
  using: "node24"
  main: "index.js"
  env:
    FOR_REAL: "1"
inputs:
  GITHUB_TOKEN:
    description: "Personal access token with full control of private repositories scope enabled"
    required: true
